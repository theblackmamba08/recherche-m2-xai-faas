# Mémoire — hpc

## 2026-04-26 — Setup initial

- Squelette créé : `slurm/`, `envs/`, `logs/` (les deux derniers gitignored sauf `.gitkeep`).
- Exemple SLURM `slurm/h1-baseline.sbatch.example` posé (à dupliquer + adapter au cluster cible).
- Cluster cible non encore identifié (Dschang ? Grid5000 ? Jean Zay ?).
- Suite → cf. [STEPS.md](STEPS.md) : choisir cluster, lister modules dispo, écrire `envs/environment.yml`.
